
// value > 0 ? "1st" : "2nd"
// here if the condition is ture then it will print 1st but if it is false then it will print 2nd

var number = Number(prompt("Enter a number: "));

var result = number > 0 ? "P1ositive" : "Negative"
document.write(result);

//number > 0 ? document.write("Positive") : document.write("Negative");//we can write this code in one line

//--------------------------------------------------------------------

/*var result = number > 0 ? "Positive" : number<0 ? "Negative" : "Zero";// as like---> if, else if, else
document.write(result);*/
